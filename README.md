# initiative-api

-- zip -r my-api.zip backend   -x "backend/node_modules/*"   -x "backend/.git/*"   -x "backend/.env*"   -x "*.swp"

## Tests

auth smoke tests
password reset
initiative entry mutation routes
batch routes
from-character/from-party routes
campaign/character ownership
encounter lifecycle
level-up happy path
level-up discard path
level-up outsider rejection

