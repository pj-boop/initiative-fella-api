ApiError:
  type: object
  required:
    - message
  properties:
    message:
      type: string
      example: Invalid credentials
MessageResponse:
  type: object
  required:
    - message
  properties:
    message:
      type: string
